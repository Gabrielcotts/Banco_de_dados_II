-- UTILIZAR UM COMENTARIO EM UMA LINHA (SINAL DE MENOS DOBRADO)
/*
SELECT
    *
FROM

PARA COMENTAR UM BLOCO, DIVERSAS LINHAS, UTILIZA /* E 
*/
-- ESTRUTURA DO COMANDO DML SELECT
SELECT -- AS COLUNAS A SEREM PROJETADAS, SEPARADAS POR V RGULA
       -- PARA PROJETAR TODAS AS COLUNAS DA ORIGEM (FROM) UILIZAR O *
FROM -- TRAZER A ORIGEM DE ONDE SER O EXTRA DOS OS DADOS
     -- PODER SER TABELAS, VIEWS, SUBQUERIES
WHERE -- PARA RESTRI  O DE LINHAS. MOSTRAR AS LINHAS QUE ATENDAM
      -- UMA CONDI  O (UTILIZAR OS OPERADORES RELACIONAIS: =, >=, <=, <> !=, >, <
      -- OPERADORES LÓGICOS: NOT, AND, OR
      -- OUTROS OPERADORES: BETWEEN, LIKE)
GROUP BY -- ORGANIZAR O RESULTADO COM SUB GRUPOS
HAVING -- PARA RESTRI  O DE SUBGRUPOS (PODE SER UTILIZADOS OS MESMOS OPERADORES
       -- DA CL USULA WHERE
ORDER BY -- UTILIZADA PARA ORDENAR O RESULTADO
; -- ; FINALIZA A QUERY

-- UITLIZAR O SELECT PARA PRODUZIR VALORES CONSTANTES. PEX: UM CALCULO
SELECT (12342423 * 424424 / 44424) AS CALCULO1,
       ROUND((12342423 * 424424 / 44424),2) CALCULO2
FROM DUAL;

-- UITLIZAR O SELECT PARA PRODUZIR VALORES CONSTANTES COM RESULTADOS DE TABELAS/VIEW
-- EXPLORANDO AS INFORMAÇÕES EXISTENTES NO DD (CATÁLOGO) DO ORACLE
SHOW USER; -- PARA MOSTRAR O USUÁRIO LOGADO

-- ACESSANDO O DICIONÁRIO DE DADOS - DD (CATÁLOGO)
-- ALL_OBJECTS É UMA VIEW QUE PERMITE VISUALIZAR TODOS OS OBJETOS QUE O USUÁRIO LOGADO PODE ACESSAR
DESC ALL_OBJECTS; -- DESC DESCREVE A ESTRUTURA DA TABELA / VIEW

  
-- CRIANDO TABELAS 

-- ALTERANDO ESTRUTURA DE TABELAS JÁ CRIADAS

-- RENOMEANDO UMA TABELA

-- RENOMEANDO UMA COLUNA DA TABELA

-- ADICIONANDO UMA COLUNA NA TABELA

-- MODIFICANDO A ESTRUTURA DE UMA TABELA (COLUNA)

-- ELIMINANDO UMA COLUNA DA TABELA

-- ADICIONANDO E ELIMINANDO CONSTRAITS (REGRAS) NA TABELA

-- VERIFICANDO A TABELA CRIADA

-- VERIFICANDO AS COLUNAS DA TABELA CRIADA NO DD (CATÁLOGO)

-- VERIFICANDO AS REGRAS (CONSTRAINTS)DA TABELA CRIADA NO DD (CATÁLOGO)

-- VERIFICANDO AS REGRAS (CONSTRAINTS)DAS COLUNAS DA TABELA CRIADA NO DD (CATÁLOGO)

-- VERIFICANDO OS ÍNDICES DA TABELA CRIADA NO DD (CATÁLOGO)

-- CRIANDO A TABELA CLIENTE DO PROJETO PEDIDO

-- CRIANDO INDICES

-- RECRIAND O INDICE COLOCANDO O MESMO EM ORDEM DESCENDENTE

-- VISUALIZANDO DETALHES DAS COLUNAS INDEXADAS
-- VERIFICANDO OS ÍNDICES DA TABELA CRIADA NO DD (CATÁLOGO)
      
-- CRIANDO SEQUENCES

--RECRIANDO A SEQUENCE COM MUDANÇAS NOS PARÂMETROS

-- VERIFICANDO A SEQUENCE CRIADA

-- UTILIZANDO A SEQUENCE

-- VERIFICANDO O VALOR ATUAL GERADO PELA SEQUENCE

-- INCLUINDO COMENTÁRIOS EM TABELAS E COLUNAS

-- VERIFICANDO COMENTÁRIOS DE TABELAS

-- INCLUINDO COMENTÁRIOS PARA COLUNAS

-- VERIFICANDO COMENTÁRIOS DAS COLUNAS DE TABELAS

-- INCLUINDO DADOS

-- INSERINDO LINHA-ALINHA NA TABELA

-- PARA DESFAZER AS ALTERAÇÕES QUE ESTÃO NA TRANSAÇÃO: ROLLBACK
-- PARA CONFIRMAR AS ALTERAÇÕES QUE ESTÃO NA TRANSAÇÃO: COMMIT
-- OBS: SE TIVER ALTERAÇÕES NA TRANSAÇÃO E REALIZAR UM COMANDO DDL, 
-- TAMBÉM SERÃO CONFIRMADAS (INCLUÍDAS) COMO NA EXECUÇÃO DO COMMIT
-- É POSSÍVEL FAZER INCLUSÕES DE PONTOS DE CONTROLE PARA ROLLBACK PARCIAL: SAVEPOINT

-- INSERINDO MULTIPLAS LINHAS EM UM COMANDO

-- JUNTANDO TABELAS
    
-- TRABALHANDO COM FUNÇÕES DE GRUPO
-- AVG, COUNT, MIN, MAX, SUM

-- ALTERANDO DADOS EM TABELAS

-- EXCLUSÃO DE REGISTROS (LINHAS) EM TABELAS

-- CRIANDO VIEWS

-- VERIFICANDO A VIEW CRIADA NO DD (CATÁLOGO)

-- REALIZANDO PLANO DE EXECUÇÃO NO SGBDR ORACLE - EXPLAIN PLAN FOR

-- TRIGGERS, FUNCTIONS E PROCEDURES
